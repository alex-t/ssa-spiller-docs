# Biweekly Report — May 2–15, 2026

## SSA Register Allocator

### Design
- Added Motivation section to design document (RFC-ready): EXEC mask correctness, occupancy control with iterative cost-model loop, bounded compilation time, IDF-based spill placement
- Discovered and resolved register width fragmentation issue: single-pass PEO coloring fails on mixed-width register files. Designed width-descending multi-pass strategy with correctness proofs for both aligned (SGPR) and unaligned (VGPR, stride=1) register files
- Designed post-coloring LR splitting for occupancy improvement: threshold-driven, widest-first, plan/commit separation with shadow simulation
- Identified cross-range accumulation problem and designed competitor re-coloring into freed slots

### Implementation
- Implemented width-descending PEO coloring with per-point liveness tracking via BitVector of register units. Handles tied operands, wider-def marking for intra-BB correctness, kill tracking at next slot
- Refactored coloring into per-width passes interleaved with splitter (`colorByWidth` + `splitForOccupancy`)
- Implemented splitter skeleton: ShadowMap (SmallVector<ShadowEntry>), `isPhysRegFreeForRange`, `findFreeInRange` (direct hw index construction via AMDGPU::VGPR0 + idx + getMatchingSuperReg), `planSplit`, `commitPlan`, `fillGaps` for partial-death subrange gaps
- Fixed multiple splitter bugs: unconditional break in inner loop, Plan.NewMaxIndex early exit, synthetic register crash (replaced DenseMap with SmallVector), Shadow not updated after split recording

### Testing
- Created 5 coloring LIT tests: basic, width-descending, reuse-after-death, width-advantage, sgpr-vgpr-mix
- Created 6 splitter test stubs plus 1 WIP scenario
- All 12 tests passing on gfx900/gfx1200
- Identified need for cross-range accumulation test scenario to exercise splitter end-to-end
- Analyzed gfx900 occupancy thresholds (256 VGPRs, granule=4, MaxWaves=10) for test design

### Infrastructure
- Recovered from working tree corruption: all allocator files and tests recreated from conversation history, committed and pushed
- Set up build configuration for ssara worktree (CMakePresets, user-debug preset)
- Restructured .cursor/ across all worktrees: shared rules/skills via symlinks, per-workspace hooks/state

## Heterogeneous DWARF Debug RFC Review
- Reviewed Heterogeneous DWARF debug information RFC for AMDGPU targets
- Provided feedback on debug location representation for divergent control flow
- Reviewed proposed extensions to DWARF standard for GPU debugging

## Next Steps
- Design and implement cross-range accumulation test for splitter
- Clean up split test files (fix incorrect scenarios/CHECK lines)
- Implement SSA-Destruct (phi to copies/swaps)
- Implement operand rewrite (vreg to physreg)
- SSA Rebuilder refactoring to MachineLaneSSAUpdater (parallel task)
