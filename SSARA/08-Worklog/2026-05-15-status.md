# Status — 2026-05-15

## Current State

- Coloring (width-descending PEO) implemented and working. 5 coloring tests pass.
- Splitter skeleton implemented: ShadowMap, per-width interleaving, plan/commit, occupancy thresholds.
- Splitter finds splits correctly (verified via debug output).
- No test yet demonstrates a reducible split scenario. 12 tests pass total.

## Issues

1. Need cross-range accumulation test: vreg spanning two epochs with non-overlapping groups, where max index exceeds threshold and splitting reduces it.
2. Several split tests have incorrect scenarios/CHECK lines — need fix or delete.
3. `color-width-advantage` CHECK lines incomplete.
4. Debug prints in planSplit need cleanup.
5. Unused variable warning (`OrigPhysReg`).
6. `fillGaps` / partial-death gap detection untested.
7. SGPR/AGPR occupancy not handled.
8. `commitPlan` not yet exercised.

## Bugs Found and Fixed This Session

- **Unconditional break** in inner loop: exited after first OtherVReg probe regardless of result. Removed.
- **Plan.NewMaxIndex == 0 early exit**: `0 <= TargetMaxIndex` always true, outer loop exited after first candidate. Fixed with `> 0` guard.
- **Synthetic registers crash**: fake register IDs caused MRI out-of-bounds. Fixed by switching Shadow from DenseMap<Register, ShadowEntry> to SmallVector<ShadowEntry>.
- **Shadow not updated after split**: competitor re-coloring saw original (unsplit) ranges, blocking valid recolors. Fixed by shortening SplitEntry.End and pushing fragment entry.

## TODO

1. Design cross-range accumulation test for gfx900
2. Fix/delete broken split test files
3. Complete CHECK lines on all tests
4. Remove unused variable
5. SSA-Destruct
6. Operand rewrite
7. Phi coalescer
8. Spiller tied-operand RP fix
9. SSA Rebuilder refactoring (parallel)
